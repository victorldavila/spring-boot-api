package com.finapp.buildsrc

object ApiVersion {
    const val version = "0.0.1"
    object Lib {
        const val core = "0.0.1"
        const val authorize = "0.0.1"
        const val security = "0.0.1"
        const val user = "0.0.1"
    }

    object Feature {
        const val auth = "0.0.1"
        const val user = "0.0.1"
    }
}